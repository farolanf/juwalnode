# Turing Full Stack Challenge

## Requirements

- Node >= 10.0.0 for full ES 2018 support

## Setup

### Backend

- `npm install` or `yarn`
- `npm start`

### Frontend

- `npm install` or `yarn`
- `npm start`